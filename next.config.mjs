/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'atougxxgxumbbnulswga.supabase.co',
      },
    ],
  },
};

export default nextConfig;
